#ifndef _LP_STRUTILS_H_
#define _LP_STRUTILS_H_

/// Returns 1 if string only contains digits, 0 otherwise.
extern int strisdigit(const char *);

/// Trims the left side of a string.
extern char *strltr(char *);

#endif // _LP_STRUTILS_H_

#ifndef _STM_STRUTILS_H
#define _STM_STRUTILS_H

/// Returns 1 if string only contains digits, 0 otherwise.
int strisdigit(const char *);

/// Trims the left side of a string.
char *strltr(char *);

#endif // _STM_STRUTILS_H
